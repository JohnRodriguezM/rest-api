const $header = document.querySelector('footer');
let ubicacion = window.pageYOffset;window.addEventListener('scroll', () => {let desplazamiento = window.pageYOffset + 2;if (ubicacion >= desplazamiento) {$header.style.bottom = '0'}else {$header.style.bottom = '-200px'
}return ubicacion = desplazamiento;});window.addEventListener('DOMContentLoaded', () => {if (ubicacion === 0) {$header.style.bottom = '-9999px';setTimeout(() => {$header.style.bottom = '0';}, 2500)}})